#!/bin/bash

config=$(kdialog --title "Switcher" --combobox "Calamares mode:" "online" "offline" --default "online")

if [[ -n "$config" ]]; then
    sudo rm -r /etc/calamares
    sudo ln -s /etc/calamares-"$config" /etc/calamares
fi
